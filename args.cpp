#include <args.h>

// static void printUsage() {
// }

bool parseArgs(int argc, char *argv[], Values& digits) {

    digits.clear();
    digits.push_back(1);
    digits.push_back(2);
    digits.push_back(3);
    digits.push_back(4);
    digits.push_back(5);
    digits.push_back(6);

    return true;
}

